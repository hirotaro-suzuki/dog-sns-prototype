# 2026-06-18 作業ログ

## 今日の目的

フェーズ1の最初として、店舗ログイン、店舗情報取得、担当者選択、既存撮影フローへの接続を通す。

## 実施したこと

### Supabase

- Supabase Organization `Dog SNS System` を作成した。
- Supabase Project `Dog-sns-prototype` を作成した。
- Regionは Southeast Asia (Singapore) を選択した。
- `supabase/schema.sql` を適用した。
- `supabase/seed.example.sql` を適用した。
- サンプル店舗と担当者を作成した。

サンプルログイン:

```text
店舗コード: DEMO
PIN: 0000
```

サンプル担当者:

```text
店長
山田
佐藤
鈴木
```

### Supabase権限

初回ログイン時に以下のエラーが出た。

```text
permission denied for table stores
```

原因は、Supabase側で `service_role` にテーブル参照権限が付いていなかったこと。

対応として、Supabase SQL Editorで以下を実行した。

```sql
grant usage on schema public to service_role;
grant select, insert, update, delete on table
  public.stores,
  public.staff_members,
  public.admin_users,
  public.assets,
  public.store_frames
  to service_role;

alter default privileges in schema public grant select, insert, update, delete on tables to service_role;
```

この手順は `supabase/schema.sql` と `supabase/README.md` に追記済み。

### Vercel

Vercel Project `dog-sns-prototype` に以下の環境変数を設定した。

```text
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
```

`SUPABASE_SERVICE_ROLE_KEY` はサーバー側だけで使う。ブラウザに出してはいけない。

### アプリ側

以下を実装した。

- Supabase接続設定
- `/store/login`
- `/api/store-login`
- `/store`
- 店舗ログインフォーム
- 店舗ホーム
- 担当者選択
- 店舗情報と担当者情報を既存の撮影フローへ渡す処理
- ログイン成功後に `/store` へ自動遷移
- 未ログインで `/store` を開いた場合のログイン導線
- ログイン失敗時の確認用メッセージ

## 確認済み

- [x] Vercelでビルド成功
- [x] `/store/login` が表示される
- [x] `DEMO / 0000` でログインできる
- [x] 店舗名 `今日のわんちゃん Demo` が表示される
- [x] 担当者4名を読み込める
- [x] ログイン後に `/store` へ遷移する
- [x] 担当者を選択できる
- [x] 撮影フローへ進める
- [x] iPadでここまで動作確認済み

## まだやらないこと

- 未許諾写真、ボツ写真、途中写真のクラウド保存
- SNS自動投稿
- 本部管理画面の本格実装
- 実店舗データの投入
- 実ロゴ、実フレームの登録

## 次に進む候補

次回は、店舗ログイン後に取得した店舗情報と担当者情報を、撮影フローの最後まで確実に持ち回る。

具体的には以下。

1. 完成画像生成時に、店舗名・担当者名が正しく反映されるか確認する。
2. 将来保存するデータとして、`store_id`、`store_display_name`、`staff_id`、`staff_display_name` を最後まで保持する。
3. ロゴ・フレームURLが未設定でも壊れない表示を確認する。
4. 実店舗向けのロゴ・フレーム登録方法を検討する。
5. 保存処理へ入る前に、最終合意確認画面の流れを設計する。

## 次回スレッドへの注意

- GitHubの `main` が最新版。
- ローカルのDropboxフォルダは見ない。
- まず `README.md` と `dog_sns_design.md` を読む。
- ユーザーは73歳の素人で、バイブコーディングにチャレンジ中。
- Codex側でできる確認、修正、記録、Vercelビルド確認はできる限りCodex側で行う。
- ユーザーに画面操作を頼む時は、一歩ずつ、専門用語を減らして案内する。
- 各セッションで、作業内容だけでなく、気づいた点や改善提案も1つ以上添える。
