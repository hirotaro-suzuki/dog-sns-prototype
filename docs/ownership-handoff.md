# 所有権・運用引き渡しメモ

最終更新: 2026-07-04

このプロジェクトは、ユーザー本人の長期事業ではなく、友人が経営する飲食店を支援するためのものである。

ある程度使える形になったら、店舗側または運用主体へ引き渡し、ユーザー本人は手を引く前提で管理する。

そのため、GitHub、Vercel、Supabaseを正として、特定のPC、Dropbox、個人フォルダ、ローカル複製リポジトリ、ユーザー本人の記憶がなくても引き継げる状態を保つ。

## 基本方針

- 最上位原則は `docs/project-principles.md` とする。
- GitHubの `main` をソースコードと文書の正本にする。
- 実行、ビルド、公開確認はVercelを正とする。
- DB、Storage、店舗設定、担当者、枠、完成画像はSupabaseを正とする。
- ロゴは単独素材ではなく、写真枠のデザイン内に含める方針にする。
- 既存のロゴURL、ロゴアップロード、ロゴ管理は、後で削除する予定の古い機能として扱う。
- ローカルPCやDropboxなど、個人所有の環境を引き継ぎ前提にしない。
- ユーザー本人が将来すべての経緯、判断、設定、操作手順を覚えている前提にしない。
- 仕様、判断理由、作業手順、未決事項、注意点はGitHub上の文書に残す。
- Codexを中心に進め、ユーザーが複数AIや複数ツールの間で大量のコピー＆ペーストをして情報を運ぶ運用に戻さない。
- 実装前に目的、OK条件、確認方法、更新すべき文書を整理する。
- 作業区切りでは、Codexからの気づき、リスク、改善案も提示する。ただし即実装せず、ユーザー確認後に候補とする。
- 本番環境の値はコードに書かず、VercelとSupabase側で差し替えられるようにする。
- 店舗担当者は日常操作だけ、本部担当者はマスタ管理と保存画像管理を担当する。
- SNS投稿そのものは後続フェーズとし、撮影、編集、同意後保存、本部確認までを疎結合に保つ。

## この文書の更新ルール

この文書は、単なるメモではなく、将来の所有権移管と運用引き渡しのための基準文書として扱う。

GitHub、Vercel、Supabase、環境変数、DB構造、Storage、管理画面、店舗画面、権限、削除方針、または引き渡し手順に関わる変更を行った場合は、作業の最後にこの文書も確認する。

## 現在の正本

- 最上位原則: `docs/project-principles.md`
- 設計書: `dog_sns_design.md`
- 次スレッド開始指示: `docs/next-thread-start.md`
- Supabase引き継ぎ: `docs/supabase-handoff.md`
- Supabaseスキーマ: `supabase/schema.sql`
- 追加SQL履歴: `supabase/migrations/`
- 2026-07-04管理画面ブラッシュアップ記録: `docs/session-2026-07-04-admin-handoff.md`
- 2026-07-04枠デザイン立て直し記録: `docs/session-2026-07-04-frame-reset.md`
- 2026-07-04所有権引き継ぎ補足: `docs/ownership-handoff-2026-07-04-frame-note.md`

この文書は所有権と運用引き渡しに関する補助文書であり、細かな画面仕様は `dog_sns_design.md` を正とする。
判断に迷った場合は、まず `docs/project-principles.md` を確認する。

## 2026-07-04時点で実装済み

店舗側:

- 店舗コード + PINで店舗ログインできる。
- 店舗情報、担当者一覧、枠、テーマ色をSupabaseから取得できる。
- 店舗ごとに最大3種類の有効な枠を表示し、撮影前に選択できる。
- ロゴ単体画像は店舗画面や完成画像へ重ねず、ロゴ入り枠画像を使う方針へ変更済み。
- iPad Safariで最大3枚まで撮影できる。
- 撮影画像は長辺2400pxを上限に軽量化し、ブラウザ内に一時保持する。
- 3枚から1枚を選び、その後で担当者を選択できる。
- 写真の移動、拡大縮小、回転、手動モザイク、短い文字入れができる。
- わんちゃん情報はDBに保存せず、写真上の短い文字として完成画像に焼き込む。
- 完成画像確認画面から、印刷、保存、編集へ戻る操作ができる。
- SNS掲載OKをもらった完成画像だけをSupabase Storageと `assets` テーブルへ保存できる。

本部側 `/admin`:

- 本部PINでログインできる。PINの値はGitHubに記録しない。
- 保存済み写真の一覧、説明文更新、非表示、復帰ができる。
- 保存済み写真に40文字以内の一言メモ `assets.short_caption` と本部確認状態 `assets.review_status` を持てるDB/API/UI土台をGitHub mainへ反映済み。
- `review_status` の内部値は `new`、`candidate`、`hold`、`rejected`。表示は `未確認`、`投稿候補`、`保留`、`使用しない`。
- 写真一覧APIは店順、日付新しい順/古い順、確認状態絞り込みに対応済み。
- 写真個別更新APIは一言メモと確認状態の更新、不正値拒否に対応済み。
- 写真タブUIで確認状態、一言メモ、確認状態フィルター、日付順/店順、日付の新しい順/古い順を操作できるようにした。
- 写真カード上に確認状態と一言メモを表示するようにした。
- 写真詳細側で確認状態と一言メモを保存できるようにした。
- 店舗マスタの表示名、SNS表示名、フレームURL、テーマカラー、有効/無効を編集できる。
- 担当者マスタの追加、名前変更、役割、有効/無効、並び順を編集できる。
- 店舗ごとの写真枠を追加、アップロード、差し替え、有効/無効、標準枠、並び順を管理できる。
- 既存の店舗ロゴ画像アップロード機能は残っているが、今後削除予定の古い管理機能として扱う。

DB・素材管理:

- `stores`、`staff_members`、`assets`、`store_frames` を中心に運用する。
- `store_frames` には、枠ごとの日付表示設定用カラムを追加済み。
- 日付表示設定カラムは、`date_enabled`、`date_x`、`date_y`、`date_font_size`、`date_color`。
- 追加SQLは `supabase/migrations/20260704_frame_date_settings.sql` に記録済み。
- `assets` には本部確認用の `short_caption` と `review_status` を追加済み。
- 追加SQLは `supabase/migrations/20260704_admin_asset_review_fields.sql` に記録し、Supabase本体にも適用済み。
- `supabase/schema.sql` にも、`short_caption`、`review_status`、制約、検索用indexを反映済み。
- `stores.logo_url` など既存ロゴ関連項目は、すぐには消さず、枠画像への移行後に整理する。

## 2026-07-04時点の注意点

- `supabase/migrations/20260704_admin_asset_review_fields.sql` はGitHub mainへ追加済みで、2026-07-04にSupabase SQL Editorで適用済み。実行結果は `Success. No rows returned`。
- Vercel上で `/admin` 写真タブの実操作確認はまだ未確認。
- iPad Safariでの `/admin` 写真タブ確認はまだ未確認。
- Canon SELPHY / AirPrintの実機印刷確認は未解決。Web標準の印刷画面を開くところまでが実装範囲。
- 店舗側で保存済み写真を日付別に確認、再印刷、非表示にする機能はまだ未実装。
- SNS投稿内容を作成、保存、管理する機能はまだ未実装。
- Instagram投稿、自動投稿、Meta API連携は、権限、アカウント管理、外部サービス連携が関わるため、このプロトタイプ内で先走って実装しない。
- 保存済み画像を使った投稿文作成、投稿素材化、Instagram投稿支援は後続フェーズとする。
- ロゴは枠画像内に含める方針へ変更済み。`/admin` に残っているロゴ管理は、後続作業で削除または非表示にする。
- 2026-07-04に追加した `public/store-frames/karuizawa-premium-*.svg` と `public/store-frames/karuizawa-simple-*.svg` は仮素材。正式ロゴを使った社長確認用の完成フレームではない。
- 2026-07-04に追加した `/admin/frame-cleanup` は一時補助画面。恒久機能として残すか、後で削除するかを決める。
- `seed.example.sql` は本番運用では使わず、新規環境の動作確認用サンプルとして残す。

## 仮の運用分担

店舗担当者は、原則として店舗側画面だけを使う。

店舗担当者の主な日常操作:

- 店舗コードとPINでログインする。
- 必要に応じて写真枠を選ぶ。
- わんちゃんを撮影する。
- 3枚の中から写真を選ぶ。
- 撮影担当者を選ぶ。
- 必要な場所だけ手動でモザイクする。
- 短い文字を写真上へ入れる。
- 完成画像を確認する。
- 印刷する。
- お客様からSNS掲載OKをもらった場合だけ保存する。

本部担当者は、原則として `/admin` を使う。

本部担当者の主な管理操作:

- 保存済み完成画像を一覧確認する。
- 保存済み完成画像の説明文を更新する。
- 保存済み完成画像の一言メモと本部確認状態を更新する。
- 保存済み完成画像を非表示、復帰する。
- 店舗マスタを編集する。
- 担当者マスタを追加、停止、編集する。
- 店舗フレームを登録、差し替えする。

後続フェーズで検討する管理操作:

- 残っているロゴ管理を削除または非表示にする。
- 複数写真の一括選択と一括更新。
- 投稿候補画像を選ぶ。
- SNS投稿素材を作成、保存するか、Manusなど別ツールへ渡す運用を整理する。
- 投稿文を確認、編集する。
- 投稿済み、保留、投稿不可などの状態を管理する。
- 完全削除やStorageファイル削除を行う。

## 削除・非表示の方針

店舗側、本部側ともに、最初は完全削除より非表示を優先する。

```text
非表示操作
→ assets.status を archived にする
→ 日常一覧には出さない
→ 本部側では確認・復帰できる
```

完全削除は、運用責任者または本部管理者だけが行う。
写真や記録を誤って消すと戻せないため、店舗側に完全削除を持たせない。

## 画面分離の方針

```text
/store または店舗側画面
店舗iPad用。撮影、枠選択、写真選択、担当者選択、編集、印刷、SNS掲載OK後の保存を行う。

/admin
本部管理用。保存画像、店舗、担当者、枠を管理する。将来はSNS投稿素材管理を追加する。
```

店舗側と本部側は同じSupabaseを使うが、画面と操作権限は分ける。
店舗側の機能追加が本部側の管理機能を壊さないようにし、本部側の投稿管理が店舗の撮影体験を重くしないようにする。

## GitHub

- ソースコードの正はGitHubの `main` とする。
- CodexはGitHub mainへ直接反映する方針とする。
- ローカルPC上の作業コピーやDropbox内ファイルを正本にしない。
- 店舗側へ引き渡す場合は、リポジトリの所有権移管または店舗側Organizationへの移動を検討する。

## Vercel

- VercelはGitHubリポジトリから自動デプロイする。
- 本番プロジェクトは、可能であれば店舗側または運用主体のVercel Team配下へ移す。
- VercelのEnvironment Variablesに設定する値は `.env.example` と対応させる。
- `ADMIN_MAINTENANCE_PIN` はVercel側で管理し、GitHubへ値を記録しない。

## Supabase

- DB、Storage、店舗設定、担当者、枠、完成画像はSupabaseに集約する。
- ロゴは新規管理対象にせず、枠画像内のデザインとして扱う。
- Supabaseプロジェクトは、可能であれば店舗側または運用主体のOrganization配下へ移す。
- Service Role Keyはサーバー側だけで使い、ブラウザへ絶対に出さない。
- anon keyはブラウザで使う前提だが、RLSでアクセス範囲を必ず制限する。
- 引き渡し前に、RLSポリシー、Storageバケット、Auth設定、バックアップ方針を確認する。
- 本番DBへ適用したSQLは、必ず `supabase/migrations/` にも記録する。

## 環境変数

現時点で想定する主な値は以下。

```text
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
ADMIN_MAINTENANCE_PIN
```

`NEXT_PUBLIC_` で始まる値はブラウザに公開される。秘密にしたい値には使わない。

## 引き渡し前チェックリスト

- `docs/project-principles.md` を読めば、プロジェクトの立ち位置、進め方、禁止事項が分かるか確認する。
- `docs/next-thread-start.md` を読めば、次のCodexスレッドが迷わず再開できるか確認する。
- ユーザー本人の記憶やローカルPCに依存しない状態になっているか確認する。
- 店舗側、本部側、将来のSNS投稿機能が疎結合になっているか確認する。
- SNS投稿やInstagram連携について、店舗側に誤って「自動でできる」と伝わらないように整理する。
- GitHubリポジトリの所有者と管理者を確認する。
- Vercelプロジェクトの所有Team、請求、環境変数を確認する。
- Supabaseプロジェクトの所有Organization、請求、APIキー、RLSを確認する。
- `.env.example` と実際のVercel環境変数が対応しているか確認する。
- SupabaseのStorageバケット、公開範囲、バックアップ方針を確認する。
- `supabase/migrations/` にあるSQLが本番DBへ適用済みか確認する。
- 本番データのバックアップと削除対応の運用者を決める。
- 店舗マスタ、担当者マスタ、枠画像の更新担当者を決める。
- 店舗側の日常操作担当者と、本部側の管理担当者を決める。
- 保存済み写真の非表示、完全削除、復旧のルールを決める。
- 本部確認状態 `new`、`candidate`、`hold`、`rejected` の運用ルールを決める。
- 緊急時にクラウド保存や本部管理画面の利用を止める手順を決める。
- Canon SELPHY / AirPrintの実機印刷手順を確認する。
- 次の運用者が、GitHub、Vercel、Supabaseだけを見て再開できるか確認する。
