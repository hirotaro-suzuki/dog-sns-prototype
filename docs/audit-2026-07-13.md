# 2026-07-13 再監査記録（Claude Fable 5）

最終更新: 2026-07-13

この文書は、`docs/audit-2026-07-11.md`（GPT5.6による全面再監査）の内容を1件ずつコードと照合して裏取りし、新しく見つかった指摘を加えた記録です。

**この文書が、引き継ぎ前の整理を1件ずつ相談して決めるためのベース文書です。** 各項目の「判断」欄が「未決」のものは、ユーザーと相談してから着手します。決まった項目は判断欄を書き換えて記録します。

## 1. 監査時点

- 基準コミット: `6e2a6d9`（lockfileと共有設定のcommit直後）
- 検証結果: `npm run build` 成功、TypeScriptエラー0、ESLint（`src` 限定）エラー0・警告13件
  - 警告の内訳: `<img>` 警告8、React Hook依存警告4、Google Fonts警告1（2026-07-11監査と同一）
- 追跡ファイルの差分なしの状態から監査を開始した
- 実Supabase、Vercel、iPad実機は今回も未確認

## 2. 前回監査（GPT5.6）の裏取り結果

**2026-07-11監査の指摘は正確で、事実誤認は見つからなかった。** 主要指摘はすべてコード上で現存を確認した。したがって同監査の指摘一覧・2026-07-11のユーザー判断（店舗保存APIは現場優先で現状維持、Admin PIN強化は引き渡し後の候補）は引き続き有効として扱う。

前回監査以降に解消した項目:

- `package-lock.json` のcommit（6e2a6d9）: 依存バージョン固定の問題は解消
- `.claude/settings.json`: 同じcommitで追跡されたが、中身が過去セッション固有の許可リストで共有に不適切だったため、**2026-07-13に追跡から外し、`.gitignore` に `/.claude/` を追加した**（本監査での対応済み事項）

## 3. 新規指摘（Fable 5が今回見つけたもの）

### 3-1. トップページ `/` がPhase 0の撮影画面のまま残っている

- 対象: `src/app/page.tsx`
- 前回監査は「`/` とmetadataにプロトタイプ表記が残る」としていたが、実際は表記だけでなく、**ログイン不要の撮影ページが丸ごと生きている**。URLを知っていれば誰でもカメラ画面を開ける（担当者がいないため保存はできない）。
- 提案: `/` は `/store` へリダイレクトする。あわせて `src/app/layout.tsx` の「Dog SNS Prototype」「Phase 0」metadataも現行名称へ更新する。
- 判断: **対応済み（2026-07-13、残骸削除パック）**。`/` は `/store` へリダイレクト、metadataは「今日のわんちゃん」へ更新。

### 3-2. 顔検出まわりは完全な死にコードで、毎回のインストールで外部ダウンロードが走る

- 対象: `src/lib/faceDetection.ts`、`src/lib/canvasMosaic.ts`、`src/components/MosaicPreview.tsx`、`face-api.js` 依存、`scripts/fetch-face-models.mjs`
- 上記3ファイルはどこからもimportされていない（import検索で確認済み）。手動モザイク方針と不一致という前回指摘のとおりだが、加えて `package.json` の `postinstall` が**npmインストールのたびにGitHubから顔検出モデルをダウンロードする**。使っていない機能のためにVercelビルドごとに外部サイトへ依存している。
- 提案: 3ファイル、`face-api.js` 依存、`postinstall` とスクリプト、`public/models` をまとめて削除する。
- 判断: **対応済み（2026-07-13、残骸削除パック）**。提案どおり全て削除。あわせて `/` 削除で不要になった `src/config/stores.ts`（`phaseZeroStore`）と、未参照だった `src/types/dog.ts` も削除した（4-2の#9の一部を先行消化）。

### 3-3. `docs/supabase-handoff.md` に内部矛盾がある

- 195行目付近: 「写真を完全に削除する（Storage画像とDBレコードを両方削除、2026-07-09）」
- 213行目付近: 「完全削除、Storageファイル削除はまだ画面編集対象外とする」
- 後者が古い記述。同じ文書内で新旧の記述が混在している。
- 提案: 古い側の記述を削除する（項目4-1の文書統一とまとめて実施可）。
- 判断: **対応済み（2026-07-13、文書統一パック）**。古い側の記述を現行仕様へ書き換えた。

### 3-4. `docs/supabase-handoff.md` の適用手順がmigration 5本分古い

- 「適用手順」（既存プロジェクトへの再適用手順）は `20260706_budoubou_square_frames.sql` で止まっている。
- 手順に含まれていないmigration:
  1. `20260707_budoubou_square_photo_frames.sql`
  2. `20260708_drop_staff_role_fields.sql`
  3. `20260708_store_frame_slots.sql`
  4. `20260710_safe_store_deletion.sql`
  5. `20260710_harden_database_functions.sql`
- 別Supabase環境へ移すときに適用漏れの原因になる。前回監査も「手順は古い」と指摘済みだが、欠落している5本を本記録で特定した。
- 提案: 適用手順を全migration一覧へ更新する。
- 判断: **対応済み（2026-07-13、文書統一パック）**。適用手順を全11本の順序付きリストへ更新し、`20260704_store_frames.sql` の欠落も補った。

### 3-5. `retakePhotos` と `cancelSession` は中身が同一の重複関数

- 対象: `src/components/CameraCapture.tsx`（260行目付近）
- 前回監査はC項目として名前だけ挙げていたが、照合の結果、**一字一句同じ処理**だった。1つに統合しても挙動は変わらない。
- 提案: 1関数に統合する。
- 判断: **対応済み（2026-07-13、残骸削除パック）**。`restartCaptureSession` として1関数に統合。

## 4. 前回監査の主要指摘の現存確認（対応するかを決める一覧）

### 4-1. 文書とソースの不整合（挙動を変えない修正）

| # | 項目 | 確認結果 | 判断 |
|---|---|---|---|
| 1 | 担当者選択のタイミング: 設計書は「写真選択後」、実装は「撮影前に必須」 | 現存。`dog_sns_design.md` 62行・125行付近が実装と逆 | **対応済み（2026-07-13）**。設計書・README・supabase-handoff・ownershipを「撮影前に選択」へ統一 |
| 2 | 印刷必須/任意: 設計書・README・supabase-handoffは「印刷後に保存」、実装は印刷任意 | 現存。直すべきは文書側（実装が新しい判断） | **対応済み（2026-07-13）**。全文書を「印刷は任意、保存の前提条件ではない」へ統一。`printed_at` の現状の意味（ボタン押下時刻、キャンセル区別なし）も設計書に明記 |
| 3 | TypeScript型に `dog_name`・`dog_breed`・`dog_age`・`staff_comment` が残る（現行DBに無い列） | 現存。`src/lib/supabase/types.ts` | **対応済み（2026-07-13）**。コード内で未参照であることを確認して4列を削除 |
| 4 | `docs/ownership-handoff.md` にL版前提の記載が残る（現行は正方形） | 現存。129行目付近 | **対応済み（2026-07-13）**。正方形が正である旨へ書き換え |
| 5 | supabase-handoffの旧 `is_active`（枠の無効化）説明 | 現存 | **対応済み（2026-07-13）**。当時の仕組みである旨の注記と、現行の「存在=有効、削除で管理」の説明を追加 |

### 4-2. 残骸・設定の整理（挙動を変えない修正）

| # | 項目 | 確認結果 | 判断 |
|---|---|---|---|
| 6 | `lint` が `eslint .` のままで `.next` 生成物まで対象になる | 現存。`eslint src` への変更とflat configへの除外追記で解消 | **対応済み（2026-07-13）**。`lint` を `eslint src` にし、flat configへ生成物の除外を明記 |
| 7 | `AdminMaintenance.tsx`（1,865行）と `globals.css`（1,526行）の肥大化 | 現存 | 未決 |
| 8 | DOM監視・500ms監視・疑似要素による後付け補修 | 現存 | **対応済み（2026-07-13、全4件撤去完了）**。①日付入力 `AdminDateInputNormalizer`: iPadでカレンダーが開かない実害を出したため削除し、`AdminDatePicker` を通常のReact実装へ作り直し（iPad実機確認済み）。②再読み込み `AdminTabReloadButton`: 500ms監視＋偽ボタン注入を削除し、本体がタブ下に直接ボタンを描画。③ログアウト `AdminLogoutButton`: sessionStorageの500ms監視を削除し、本体の上部バーへ移設。④見出しすり替え: 疑似要素CSSを削除し、実HTMLの見出しを「本部管理画面」へ変更。これで `/admin` のDOM監視・定期監視・疑似要素補修はゼロ |
| 9 | `phaseZeroStore`、ブラウザ用Supabase client、重複CSS・SVG等の未使用物 | 現存 | **対応済み（2026-07-13）**。`phaseZeroStore`・`types/dog.ts`・ブラウザ用Supabase client（`src/lib/supabase/client.ts`、参照ゼロ確認済み）を削除。`globals.css` から未使用クラス10個（`admin-create-panel`、`admin-main-grid`、`color-selector`、`compact-button`、`compact-section-heading`、`frame-preview-box`、`segmented-control`、`store-logo-badge`、`store-settings-panel`、`text-editor-panel`）と `.text-count` の二重定義を削除。**残す判断をしたもの**: `public/store-frames/` のSVGはSupabaseの `frame_url` から参照されている可能性があり、Codespacesからは実DBを確認できないため削除しない（削除するならSupabase側で `store_frames.frame_url` と `stores.logo_url` を確認してから） |

### 4-3. 挙動変更を伴う候補

| # | 項目 | 確認結果 | 判断 |
|---|---|---|---|
| 10 | 写真完全削除がStorage削除の失敗を無視してDB削除へ進む | 現存。`src/app/api/admin/assets/[id]/route.ts` のDELETEで削除結果を確認していない。**挙動変更候補の中では最優先のおすすめ** | **対応済み（2026-07-13）**。Storage削除が失敗したら完全削除を中止してエラーを返すよう修正。ファイルが既に存在しない場合はエラーにならず削除を続行できる（Supabaseのremoveは存在しないパスをエラーにしないため） |
| 11 | 内部枠名 `frame-日時` が店舗画面に表示される | 現存。`CameraCapture.tsx` が `frame_name` をそのまま表示 | **対応済み（2026-07-13）**。枠選択ボタンを枠画像そのもののサムネイルに変更。枠に「季節枠・通常枠」等の区分は設けず、単に複数枠から自由に選ぶ仕様のまま |
| 12 | `/store` 再読み込み時だけ中間ホームに戻る | 現存。`?start=capture` の有無による分岐 | **対応済み（2026-07-13）**。中間ホームを廃止し、ログイン済みなら常に撮影画面を表示（`?start=capture` の仕組み自体を削除）。ホームにあった「SNS掲載OKまで保存しない」説明はログイン画面へ移設 |
| 13 | 印刷ボタン押下時点で `printed_at` 用時刻が入り、印刷ダイアログのキャンセルと区別できない | 現存。`MosaicCanvas.tsx` | **現状維持で確定（2026-07-13）**。正確にするには印刷後の確認タップが必要になり、店頭の手間を増やさない方針と合わないため。制約は設計書に明記済み |
| 14 | デモ店舗ログインが枠レコードをDBへ自動作成する | 現存。`store-login` API の `ensureDemoFrames`。デモ店舗（`DEMO_STORE`）限定で、ログイン時に枠が3つ未満なら自動補充する。ログインでDB書き込みが起きる・Adminで消しても復活する・アクセス元URLごとに枠が重複しうる、が問題点 | **保留（2026-07-13、後日改めて相談）** |
| 15 | 写真一覧160件上限が画面に出ない | 現存 | **対応済み（2026-07-13）**。一覧が160件に達したときだけ「一度に160件まで。絞り込みで表示できる」旨の注記を一覧上部に表示 |
| 16 | 枠並べ替え・標準枠変更が非トランザクション、枠StorageOrphan | 現存 | **保留（2026-07-13）**。実害が出たら着手 |
| 17 | 保存APIの画像実体・寸法・回数・二重送信対策 | 現存（認証方式自体は2026-07-11合意により現状維持） | 未決 |
| 18 | `final-images` bucketがpublicでURLを推測しやすい | 現存 | **保留（2026-07-13）**。SNS掲載OKの写真しか保存しない設計のため緊急性は低い。Admin PINと同様、引き渡し時に先方へ明示する項目とする（4-4へ追加） |

### 4-4. 合意済み・引き渡し後の項目（今は着手しない）

| 項目 | 状態 |
|---|---|
| 店舗保存APIの正式認証 | 2026-07-11合意により現状維持。技術リスクとして記録のみ |
| Admin PIN強化（静的PIN・回数制限なし・権限区分なし） | 引き渡し後の候補。引き渡し時に先方へ明示する |
| `final-images` bucketのpublic設定（4-3の#18） | 2026-07-13判断。引き渡し時に先方へ明示する |
| SNS自動投稿・Instagram連携 | 次フェーズ以降 |

## 5. おすすめの進め方（Fable 5の意見）

1件ずつ決める前提で、まとめやすい単位の案:

1. **文書統一パック**（4-1の全部 + 3-3 + 3-4）: 挙動を変えず、引き継ぎ効果が最大。1回の作業でまとめられる。
2. **残骸削除パック**（3-1 + 3-2 + 3-5 + 4-2の#6）: ビルドが軽くなり、次の人が読むコードが減る。動作確認は店舗フローの通し確認のみ。
3. **完全削除のStorageエラー処理**（4-3の#10）: 唯一すぐやる価値のある挙動変更。修正は小さい。
4. 4-3の残りは、実運用の困り具合を見ながら個別判断。
5. 4-2の#7・#8（Admin分割）は規模が大きいので、単独スレッドで扱う。

## 6. この監査で実施した変更

- `.claude/settings.json` を追跡から外し、`.gitignore` へ `/.claude/` を追加（2026-07-13）
- `docs/audit-2026-07-13.md`（本文書）の追加と `docs/DOC_INDEX.md` の更新
- **文書統一パックを実施（2026-07-13）**: 4-1の5項目と3-3・3-4を対応済みへ。対象は `dog_sns_design.md`、`README.md`、`docs/supabase-handoff.md`、`docs/ownership-handoff.md`、`src/lib/supabase/types.ts`（未使用の型4列削除のみ、挙動変更なし）
- **残骸削除パックを実施（2026-07-13）**: 3-1・3-2・3-5・4-2の#6を対応済みへ。face-api一式と `postinstall`、`/` のPhase 0ページ（`/store` へリダイレクト化）、旧metadata、重複関数、`phaseZeroStore`、`types/dog.ts` を削除し、`lint` を `eslint src` + 生成物除外へ変更
- **写真完全削除のStorageエラー処理を修正（2026-07-13）**: 4-3の#10を対応済みへ
- **店舗側UI改善2件を実施（2026-07-13）**: 4-3の#11（枠選択ボタンのサムネイル化）と#12（中間ホーム廃止）を対応済みへ
- **Admin日付入力のiPad不具合を修正（2026-07-13）**: ユーザー報告の「iPadでカレンダーが開かない」を、後付け補修の削除と `AdminDatePicker` の作り直しで対応（4-2の#8の一部を消化）。**同日、iPad実機でカレンダー動作を確認済み**
- **残りの後付け補修3件を撤去（2026-07-13）**: 再読み込みボタン・ログアウトボタン・見出しすり替えを通常のReact実装へ戻し、4-2の#8を完了へ
- **残骸削除の続きと160件注記を実施（2026-07-13）**: 4-2の#9を対応済みへ（ブラウザ用Supabase client削除、未使用CSSクラス10個と二重定義の整理。枠SVGはDB参照未確認のため残置）。4-3の#15を対応済みへ（160件到達時の注記表示）。同日、#13は現状維持で確定、#14は保留（後日相談）、#16は保留（実害待ち）、#18は保留（引き渡し時に明示、4-4へ追加）と判断

上記以外に、DB・Storageは変更していない。
