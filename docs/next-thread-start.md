# 次のClaude Codeスレッド開始時の指示文

次のClaude Codeスレッド（引き継ぎ資料の作成）を始めるときは、最初に以下をそのまま貼る。

```text
作業対象は hirotaro-suzuki/dog-sns-prototype の main です。
作業環境は、ユーザーが開いているGitHub Codespacesの `/workspaces/dog-sns-prototype` を正として扱ってください。
Claude Codeは、自身のツールを用いて直接ファイルを編集し、コミット・pushなどのGit操作を行ってください。
実装、ビルド、コミット、pushはClaude Code自身がCodespacesターミナルで行います。
公開、デプロイ、ビルド結果の確認はVercelを正として扱ってください（本番URL: https://dog-sns-prototype.vercel.app ）。
DB、Storage、Auth、店舗設定、担当者、枠、完成画像はSupabaseを正として扱ってください。
ローカルPC、Dropbox、手元コピーは参照しないでください。
リポジトリ検索に頼らず、まず docs/START_HERE.md と docs/DOC_INDEX.md を直接取得して読んでください。
まだファイル変更はしないでください。

今回の目的: いよいよ引き継ぎ資料の作成に着手する。

現在の状況（2026-07-19時点）:
- 引き継ぎ前の監査は docs/audit-2026-07-13.md が正。全項目に判断が付き、未決ゼロ。
  保留2件（枠操作のトランザクション化 #16、final-imagesの公開範囲 #18）は
  「引き渡し時に明示する」と決めた項目なので、引き継ぎ資料に必ず記載する。
- 大型ファイル分割（audit 4-2 #7）は2026-07-19に完了。同日、店舗側の編集画面の正方形修正、
  文字入力の作り直し（下段1行＝アイコン→チェック→白パネル、白黒2色）、カメラ解像度の
  引き上げ（ピンボケ状の写り解消）まで、iPad実機（iPadOS 26.5.2）で確認済み。
  経緯は docs/session-2026-07-19-admin-split.md を読む。
- DB定義・Supabase実環境は変更していない。migrationの適用状況・手順は
  docs/supabase-handoff.md（2026-07-13更新）を正とする。
- Admin PIN強化は「引き渡し後の課題」とする判断済み（2026-07-11）。資料にもそう書く。

引き継ぎ資料づくりの進め方:
1. まず docs/ownership-handoff.md と docs/production-readiness-checklist.md を読む。
   引き継ぎの土台はこの2つ。ゼロから新しい文書を書くのではなく、既存文書を本体として
   育てる方針を基本とし、変える場合は理由を添えて提案する。
2. 書き始める前に、ユーザーへ最初に確認すること:
   - 誰に引き渡すのか（技術がわかる人か、店舗の運用担当者か、その両方か）
   - 引き渡しの時期の目安
   相手と時期で資料の書き方・分量・優先順位が変わるため、ここを最初に合意する。
3. そのうえで資料の構成案（章立てと各章のねらい）を先に提示し、合意してから書き始める。
   一気に全部書かず、章ごとにこまめにコミット・pushする。

引き継ぎ資料と並行して判断する残りの実務:
- /admin 各タブの実画面確認（2026-07-19の分割後は未確認。挙動を変えない移動のみなので
  通常確認でよい。店舗フローは2026-07-19のテストで確認済み）
- /admin の再読み込み・ログアウト・見出しの実画面確認（2026-07-13変更分）
- 写真完全削除の正常系動作（不要なテスト写真1枚で確認）
- 枠アップロードの寸法チェックの実画面確認（横長画像の拒否、正しい1080×1080の受入）
- Supabase上のデモ店舗（DEMO_STORE）の有無確認と、残っていればAdminからの削除。
  その後 public/store-frames/ の未参照SVG整理を検討（audit 4-2 #9の残置メモ参照）

進め方のルール:
- ユーザーのアイデア・質問には、まず意見や懸念を言葉で返す。「どう思う？」「意見を聞かせて」と
  言われたターンでは意見だけを返し、実装や選択肢ダイアログに進まない。
  やるかどうかはユーザーが決める（CLAUDE.md参照）。
- 作業中はキリの良い単位でこまめにコミット・pushする（Codespacesが予告なく停止するため）。
- 作業が一区切りしたら、スレッドを閉じる前に docs/next-thread-start.md を毎回更新する。
```

このファイルは、ユーザーが次スレッドの最初に貼るための開始指示を置く場所です。
作業が一区切りするたびに、Claude Codeがこのファイルを最新の状況へ更新する。

詳しい開始ルールは `docs/START_HERE.md`、読む文書の分類は `docs/DOC_INDEX.md` を正とします。
