# 次のClaude Codeスレッド開始時の指示文

次のClaude Codeスレッドで開発を再開するときは、最初に以下をそのまま貼る。

```text
作業対象は hirotaro-suzuki/dog-sns-prototype の main です。
作業環境は、ユーザーが開いているGitHub Codespacesの `/workspaces/dog-sns-prototype` を正として扱ってください。
Claude Codeは、自身のツールを用いて直接ファイルを編集し、コミット・pushなどのGit操作を行ってください。
実装、ビルド、コミット、pushはClaude Code自身がCodespacesターミナルで行います。
公開、デプロイ、ビルド結果の確認はVercelを正として扱ってください。
DB、Storage、Auth、店舗設定、担当者、枠、完成画像はSupabaseを正として扱ってください。
ローカルPC、Dropbox、手元コピーは参照しないでください。
リポジトリ検索に頼らず、まず docs/START_HERE.md と docs/DOC_INDEX.md を直接取得して読んでください。
まだファイル変更はしないでください。

現在の状況（2026-07-18時点）:
- 引き継ぎ前の監査は docs/audit-2026-07-13.md が正。全項目に判断が付き、未決はゼロ。
  保留（実害・引き渡しまで動かない）: 枠操作のトランザクション化(#16)、final-imagesの公開範囲(#18)。
- 2026-07-18に店舗側UIの総点検を実施し、指摘10件のうち1〜8を実装済み。
  経緯・実装内容・残り2件（低優先）は docs/session-2026-07-18-store-ui-polish.md を読む。
  実装済みの要点: やり直し（ゴミ箱）とログアウトの確認ダイアログ、ログアウトの退出アイコン化、
  担当者未選択と写真3枚時の案内表示、枠選択バーの編集画面限定（保存枠URLの食い違い防止）、
  モザイク描画の軽量化、文字パネルの枠外タップで閉じる、未ログイン時のログイン画面直行。
- DB定義・Supabase実環境は変更していない。migrationの適用状況・手順は
  docs/supabase-handoff.md（2026-07-13更新）を正とする。

次にやること:
- まず下の「未確認のまま残っている項目」のVercel/iPad実機確認を片付けるのがおすすめ。
- Supabase上にデモ店舗（DEMO_STORE）が残っていれば、Adminの店舗削除で片付けてもらう。
  その後 public/store-frames/ の未参照SVG整理を検討できる（audit 4-2 #9の残置メモ参照）。
- 店舗側UIの低優先2件（アイコンの意味の重複整理、リセットアイコンの変更）は必要になったら。
- 大物: AdminMaintenance.tsx（約1,900行）と globals.css（約1,500行）の分割（audit 4-2 #7）。
  分割は挙動を変えない整理として単独スレッドで行う。
- どれに着手するかは、まずユーザーに確認する。

作業中はキリの良い単位（1機能・1画面など）でこまめにコミット・pushしてください
（Codespacesが予告なく停止することがあるため）。ユーザーのアイデア・提案には、まず意見や
懸念点を言葉で返してから合意して実装してください（CLAUDE.md参照）。
作業が一区切りしたら、スレッドを閉じる前に docs/next-thread-start.md を毎回更新してください。

未確認のまま残っている項目:
- 2026-07-18の店舗側UI改善のVercel実画面確認（iPad実機推奨）:
  ゴミ箱・ログアウトで確認ダイアログが出てキャンセルできること、
  担当者未選択時と写真3枚時の案内が出ること、完成画面で枠選択バーが消えること
  （編集へ戻ると再表示）、文字パネルが枠外タップで閉じること、
  セッション切れで /store を開くと直接ログイン画面になること、
  モザイクを広く塗っても引っかからないこと
- 2026-07-13の店舗側UI変更（枠ボタンの画像化、中間ホーム廃止）のVercel実画面確認
- /admin の再読み込み・ログアウト・見出し（後付け撤去後）の実画面確認
- 写真完全削除の正常系動作（デプロイ後、不要なテスト写真1枚で確認）
- 枠アップロードの寸法チェックの実画面確認（横長画像で拒否メッセージが出ること、
  正しい1080×1080の枠が今までどおり登録できること）
- Supabase上のデモ店舗（DEMO_STORE）の有無確認と、残っていればAdminからの削除
  （自動補充の削除により、残っているとデモ店舗の枠画像だけリンク切れになる。実店舗には影響なし）
```

このファイルは、ユーザーが次スレッドの最初に貼るための開始指示を置く場所です。
作業が一区切りするたびに、Claude Codeがこのファイルを最新の状況へ更新する。

詳しい開始ルールは `docs/START_HERE.md`、読む文書の分類は `docs/DOC_INDEX.md` を正とします。
