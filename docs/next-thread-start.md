# 次のClaude Codeスレッド開始時の指示文

次のClaude Codeスレッドで開発を再開するときは、最初に以下をそのまま貼る。

```text
作業対象は hirotaro-suzuki/dog-sns-prototype の main です。
作業環境は、ユーザーが開いているGitHub Codespacesの `/workspaces/dog-sns-prototype` を正として扱ってください。
Claude Codeは、自身のツールを用いて直接ファイルを編集し、コミット・pushなどのGit操作を行ってください。
実装、ビルド、コミット、pushはClaude Code自身がCodespacesターミナルで行います。
公開、デプロイ、ビルド結果の確認はVercelを正として扱ってください。
DB、Storage、Auth、店舗設定、担当者、枠、完成画像はSupabaseを正として扱ってください。
ローカルPC、Dropbox、手元コピーは参照しないでください。
リポジトリ検索に頼らず、まず docs/START_HERE.md と docs/DOC_INDEX.md を直接取得して読んでください。
まだファイル変更はしないでください。

現在の状況（2026-07-07時点、確認済み）:
- 正方形キャンバス化、葡萄房の実ロゴ入り正方形枠、完成画像の日付表示、Admin「枠」タブの並び順修正、
  Admin「担当者」タブの所属店舗表示、Admin「店舗/担当者/枠」タブの停止中フィルターは、
  すべてGitHub main反映・Vercelデプロイ・Supabase migration適用まで完了し、ユーザーが動作確認済みです。
  詳細は docs/session-2026-07-06-square-canvas.md を参照してください。
- 担当者を店舗停止と連動して自動停止する案は、複数店舗を掛け持ちする担当者がいるため、
  あえて実装しないことでユーザーと合意済みです。

次にやること（本題）:
店舗側の撮影画面（src/components/CameraCapture.tsx）のUI/UXブラッシュアップです。
ユーザーから「見た目とオペレーションがダサい」という指摘があり、次を提案済みです。
1. シャッターボタンを丸型・大きめの専用デザインにする
2. ボタンの役割ごとに階層をつける（主操作1つを目立たせ、副次操作は控えめにする）
3. 主要アクションにアイコンを追加する
4. 進行ステップが分かるインジケータを追加する
5. 枠選択ボタンをテキストのみからサムネイル画像入りに変更する

ユーザーはまだこの画面を実機で細かく確認していません。
作業に入る前に、目的、OK条件、確認方法、更新する文書を短く整理してユーザーに確認してください。

未確認のまま残っている項目:
- iPad Safariでの撮影から保存までの通し確認（個別要素は確認済みだが全工程ではまだ確認していない）
- 新しい正方形枠でのSNS掲載OK後の保存・印刷の見え方
- 印刷時にAirPrintやSELPHY側で期待通り扱えるか
```

このファイルは、ユーザーが次スレッドの最初に貼るための開始指示を置く場所です。

詳しい開始ルールは `docs/START_HERE.md`、読む文書の分類は `docs/DOC_INDEX.md` を正とします。
