# 次のCodexスレッド開始時の指示文

次のCodexスレッドで開発を再開するときは、最初に以下をそのまま貼る。

```text
GitHub の hirotaro-suzuki/dog-sns-prototype の main が最新版です。
GitHub main を正として作業してください。

まず GitHub 上の以下を必ず読んでください。

1. README.md
2. dog_sns_design.md
3. docs/next-thread-start.md
4. docs/ownership-handoff.md
5. docs/session-2026-07-04-frame-reset.md
6. 必要に応じて docs/supabase-handoff.md、docs/session-2026-06-25.md

dog_sns_design.md が基本設計書です。
ただし、2026-07-04の枠デザイン作業では混乱と失敗があったため、docs/session-2026-07-04-frame-reset.md を必ず読んでから判断してください。

このプロジェクトは、GitHub、Vercel、Supabase を正として進めます。
ローカルPC上の作業環境や複製リポジトリを前提にしないでください。
クラウド中心にしている最大の理由は、次のCodexスレッドや別作業者へ引き継ぎやすくするためです。
引き継ぎに手間のかかる構成や、特定のPC・特定フォルダ・特定個人の手元環境に依存する開発は避けてください。

開発判断はCodexを中心に進めます。
以前のように、ターミナル画面を別AIへ渡して判断を仰ぐ運用は前提にしません。
将来SNS投稿文の自動生成などでAI機能が必要になった場合は、その時点で用途と設計を整理してから検討します。

現在の大きな方針は以下です。

- 店舗iPadは、店舗コード + PINでログインする
- 店舗情報、担当者一覧、写真枠、表示設定はSupabaseから取得する
- ロゴは単独素材ではなく、写真枠のデザイン内に含める方針にする
- 既存のロゴURLやロゴ管理は後で削除する予定の古い機能として扱う
- 撮影画像は最大3枚まで、長辺2400pxを上限に軽量化してブラウザ内に一時保持する
- 写真選択後に担当者を選択する
- ワンチャン情報はDBに保存しない
- ワンチャン情報は、写真上に自由配置する短い文字として完成画像に焼き込む
- モザイクと文字は写真に連動して動く
- 印刷は完成画像確認画面の印刷ボタンから行う
- クラウド保存するのは、お客様からSNS掲載OKをもらった完成画像だけにする
- 撮影途中の写真、選ばれなかった写真、未許諾写真、キャンセル写真はクラウドへ送らない
- 完成画像確認画面の保存ボタンから、Supabase Storageとassetsテーブルへ保存できる
- `/admin` で保存済み写真の一覧、説明文更新、非表示/復帰ができる
- `/admin` で店舗マスタ、担当者マスタ、写真枠を編集できる
- `/admin/frames` で、枠名と画像を選び1回のボタンで写真枠を登録できる
- `/admin/frame-cleanup` は一時補助画面として追加された。恒久機能として扱うか削除するかは再検討する
- seed.example.sql は本番運用では使わず、新規環境の動作確認用サンプルとして残す

2026-07-04時点の重要な補足:

- 軽井沢店の枠作業で混乱があった。
- ユーザーが求めているのは、正式ロゴを枠内に入れた、社長に見せられる見栄えのよいL版フレーム。
- 現在GitHubにある `public/store-frames/karuizawa-premium-*.svg` は正式ロゴ画像を使っていないため、完成版として扱わない。
- 以前、問題回避を急いで単純な枠へ寄せたが、それはユーザーの目的と違っていた。
- 次はSNS投稿素材機能へ進まず、まず枠デザインを立て直す。
- まず軽井沢店の正式ロゴ入りフレームを1色だけ完成品質まで作り、iPadで撮影画面、編集画面、完成画像、印刷見えを確認する。
- その後に2色目、本店用へ広げる。
- 店舗ログイン時のテスト枠自動追加は `DEMO_STORE` のみに制限済み。以前は `DEMO_KARUIZAWA` にも効いて古いテスト枠が復活していた。

次に進む候補は、本来は保存済み写真からSNS投稿内容を作成・保存する機能です。
しかし、その前にまず軽井沢店の正式ロゴ入りフレームを見せられる品質に立て直してください。
先走ってSNS投稿素材機能へ進まないでください。

私は73歳の素人で、バイブコーディングにチャレンジ中です。
できる限り、SQL確認、GitHub確認、Vercel確認、コード修正、手順整理などはCodex側で進めてください。
私が画面操作しないと進められない場合だけ、専門用語を減らして一歩ずつ案内してください。

各セッションの終わりや区切りでは、作業内容の記録に加えて、気づいた点や次に良くなる提案を1つ以上ください。
先走って実装せず、読んだ内容と次に進む候補を確認してから止まってください。
```

## 短く始めたい場合

```text
hirotaro-suzuki/dog-sns-prototype の GitHub main を正として、README.md、dog_sns_design.md、docs/session-2026-07-04-frame-reset.md を必ず読んで続きから。クラウド中心、引き継ぎやすさ優先、Codex一本で進める方針です。2026-07-04の枠デザイン作業では混乱があり、正式ロゴを使わない仮枠へ寄ってしまったため、次はSNS投稿素材機能へ進まず、まず軽井沢店の正式ロゴ入りL版フレームを社長に見せられる品質で作り直してください。私は73歳の素人なので、できる限りCodex側で進め、必要な画面操作だけ専門用語を減らして案内してください。
```
