# 次のClaude Codeスレッド開始時の指示文

次のClaude Codeスレッドで開発を再開するときは、最初に以下をそのまま貼る。

```text
作業対象は hirotaro-suzuki/dog-sns-prototype の main です。
作業環境は、ユーザーが開いているGitHub Codespacesの `/workspaces/dog-sns-prototype` を正として扱ってください。
Claude Codeは、自身のツールを用いて直接ファイルを編集し、コミット・pushなどのGit操作を行ってください。
実装、ビルド、コミット、pushはClaude Code自身がCodespacesターミナルで行います。
公開、デプロイ、ビルド結果の確認はVercelを正として扱ってください。
DB、Storage、Auth、店舗設定、担当者、枠、完成画像はSupabaseを正として扱ってください。
ローカルPC、Dropbox、手元コピーは参照しないでください。
リポジトリ検索に頼らず、まず docs/START_HERE.md と docs/DOC_INDEX.md を直接取得して読んでください。
まだファイル変更はしないでください。

現在の状況（2026-07-13時点）:
- 引き継ぎに向けた全面再監査と整理を2026-07-13に実施済み。
  **経緯と残項目は docs/audit-2026-07-13.md を最初に読む**（「判断: 未決」の項目が残作業）。
- 実施済み: 文書と実装の食い違い解消（担当者選択は撮影前、印刷は任意など）、
  死にコード削除（顔検出一式、旧トップページ、重複関数）、写真完全削除のStorageエラー処理、
  店舗側UI改善（枠選択ボタンの画像化、中間ホーム廃止）、
  /admin の後付けDOM補修4件の撤去（日付カレンダーのiPad不具合修正を含む。iPad実機確認済み）。
- DB定義・Supabase実環境は変更していない。migrationの適用状況・手順は
  docs/supabase-handoff.md を2026-07-13に更新済み。

次にやること:
- 残りの未決項目は docs/audit-2026-07-13.md の表を正とする。
  軽いもの: デモ店舗ログインのDB書き込み(#14)、写真160件上限の表示(#15)。
  挙動変更系: 枠操作のトランザクション化(#16)、保存APIの容量・二重送信対策(#17)、
  final-imagesの公開範囲(#18)。
  大物: AdminMaintenance.tsx（約1,900行）と globals.css（約1,550行）の分割（4-2 #7）。
  分割は挙動を変えない整理として単独スレッドで行う。
- どれに着手するかは、まずユーザーに確認する。

作業中はキリの良い単位（1機能・1画面など）でこまめにコミット・pushしてください
（Codespacesが予告なく停止することがあるため）。ユーザーのアイデア・提案には、まず意見や
懸念点を言葉で返してから合意して実装してください（CLAUDE.md参照）。

未確認のまま残っている項目:
- 2026-07-13の店舗側UI変更（枠ボタンの画像化、中間ホーム廃止）のVercel実画面確認
- /admin の再読み込み・ログアウト・見出し（後付け撤去後）の実画面確認
- 写真完全削除の正常系動作（デプロイ後、不要なテスト写真1枚で確認）
```

このファイルは、ユーザーが次スレッドの最初に貼るための開始指示を置く場所です。

詳しい開始ルールは `docs/START_HERE.md`、読む文書の分類は `docs/DOC_INDEX.md` を正とします。
