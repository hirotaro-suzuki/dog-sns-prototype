import { AdminFrameCleanup } from "@/components/AdminFrameCleanup";

export default function AdminFrameCleanupPage() {
  return (
    <main className="app-shell">
      <section className="workspace admin-workspace">
        <AdminFrameCleanup />
      </section>
    </main>
  );
}
